---
import { Icon } from 'astro-icon/components';
interface Props {
    class?: string;
    swiper?: string;
}
const { swiper, class: className } = Astro.props;
---

<div
    class:list={[
        swiper && `${swiper}__prev`,
        'absolute right-auto top-1/2 z-10 flex -translate-y-1/2 cursor-pointer items-center justify-center opacity-0 transition-opacity duration-300 group-hover:opacity-100',
        className ? className : 'text-primary left-fl-space-m',
    ]}
>
    <Icon name="lucide:chevron-left" class="h-fl-space-l w-auto" />
</div>
