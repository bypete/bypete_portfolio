---
interface Props {
    isHeroDark?: boolean;
    class?: string;
}
const { isHeroDark, class: className } = Astro.props;
const shade = isHeroDark ? 'bg-underlay-dark/90' : 'bg-underlay-light/90';
---

<div
    id="underlay"
    class:list={[
        'fixed inset-0 z-underlay hidden h-full min-h-screen w-full backdrop-blur-sm backdrop-grayscale',
        className,
        shade,
    ]}
    x-cloak
    data-off-canvas-sticky
>
</div>
