---
interface Props {
    tags: string[];
    url?: boolean;
    class?: string;
}
const { tags, url = false, class: className = ' ' } = Astro.props;
import { Icon } from 'astro-icon/components';
// Move 'featured' to the beginning of the array, if it exists
if (tags.includes('featured')) {
    tags.unshift(tags.splice(tags.indexOf('featured'), 1)[0]);
}
---

{
    tags && (
        <ul
            class:list={[
                'flex w-full flex-wrap items-center gap-fl-space-2xs text-fl-step--1 leading-none',
                className,
            ]}
        >
            {tags.map((tag) => (
                <li>
                    {url ? (
                        <a
                            href={`/tags/${tag}`}
                            class=" inline-flex items-center gap-x-0.5 whitespace-nowrap rounded-tight bg-tag-bg__link px-fl-space-2xs py-fl-space-3xs font-medium lowercase leading-none text-tag hover:bg-tag-bg__hover hover:ring-accent"
                        >
                            {tag === 'featured' && (
                                <Icon
                                    name="mdi:star"
                                    class="-my-fl-space-3xs -ml-fl-space-3xs h-fl-space-s w-auto text-yellow-500"
                                />
                            )}
                            {tag}
                        </a>
                    ) : (
                        <span class="inline-flex items-center gap-x-0.5 whitespace-nowrap rounded-tight bg-tag-bg px-fl-space-2xs py-fl-space-3xs font-medium lowercase leading-none">
                            {tag === 'featured' && (
                                <Icon
                                    name="mdi:star"
                                    class="h-fl-space-sw-auto -my-fl-space-3xs -ml-fl-space-3xs text-yellow-500"
                                />
                            )}
                            {tag}
                        </span>
                    )}
                </li>
            ))}
        </ul>
    )
}
