---
import { Icon } from 'astro-icon/components';
interface Props {
    name: string;
    url: string;
    platform: string;
    wrap?: string;
    css?: string;
}
const {
    platform,
    url,
    name,
    wrap = 'text-content-light hover:text-brand mx-2',
    css = 'w-auto h-6 fill-current',
} = Astro.props;
---

<a href={`${url}`} rel="nofollow" class={wrap} title={platform}>
    <Icon {name} class={css} />
</a>
