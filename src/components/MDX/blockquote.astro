---
interface Props {
    class?: string;
}
const { class: className, ...attrs } = Astro.props;
---

<Fragment>
    <blockquote {...attrs} class:list={['-mx-viewport-safe-inline', className]}>
        <slot />
    </blockquote>
</Fragment>
