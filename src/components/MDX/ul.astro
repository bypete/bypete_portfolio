---
interface Props {
    class?: string;
}
const { class: className, ...attrs } = Astro.props;
---

<Fragment>
    <ul {...attrs} class:list={['list list--bullet not-prose', className]}>
        <slot />
    </ul>
</Fragment>
