---
interface Props {
    class?: string;
}
const { class: className, ...attrs } = Astro.props;
---

<Fragment>
    <pre
        {...attrs}
        class:list={[
            '-mx-viewport-safe-inline my-fluid-l rounded p-fluid-m',
            className,
        ]}><slot /></pre>
</Fragment>
